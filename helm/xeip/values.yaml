replicaCount: 3

image:
  repository: xeip-api
  tag: "1.0.0"
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 80

resources:
  requests:
    cpu: 250m
    memory: 512Mi
  limits:
    cpu: "1"
    memory: 1Gi

env:
  XEIP_ENV: production
  QDRANT_URL: http://qdrant:6333

